import modelmediacontents from "#models/mediacontents.js";
const debug = suu.createDebug('suu:loadimages:hook:contents:managecontents');

suu.on({
    
    /**
     * Save image to database
     * @param {Modelparams} imagedata
     * @return {Promise<void>}
     */
    'loadimages:source:saveimage': async(imagedata) => {
    
    },
    
});